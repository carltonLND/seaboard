{
  "name": "types",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc --outDir dist",
    "lint": "eslint .",
    "clean": "rimraf ./dist ./node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.5.2",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "rimraf": "^3.0.2",
    "typescript": "^5.0.2"
  }
}
